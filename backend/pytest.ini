[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    integration: requires real PostgreSQL + Redis (deselect with -m "not integration")
    e2e: requires full running backend + live OpenAI API (deselect with -m "not e2e")
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
